fastapi==0.141.1
uvicorn[standard]==0.30.6
pydantic==2.13.5
email-validator==2.2.0
pydantic-settings==2.14.1
sqlalchemy[asyncio]==2.0.35
asyncpg==0.31.0
alembic==1.19.2
redis==5.0.8
litellm==1.100.1
httpx==0.28.1
tenacity==9.0.0
pyjwt[crypto]==2.13.0
# python-jose[cryptography] was previously listed here but never actually
# imported anywhere in app/ (app/identity/base.py and factory.py both use
# pyjwt, the "jwt" module) -- removed rather than patched, along with its
# transitive ecdsa dependency (which carries an unfixed advisory upstream).
python-multipart==0.0.32
structlog==24.4.0

# Secret providers (app/secrets/) -- each provider's SDK is imported lazily inside
# its own module, so a deployment only pays for the one it actually uses at
# runtime; all five are still installed so every provider is testable/importable.
# cryptography backs PostgresSecretProvider's at-rest encryption (Fernet); the
# others are external-service SDKs.
cryptography==50.0.1
boto3>=1.43.1
google-cloud-secret-manager==2.21.1
azure-keyvault-secrets==4.9.0
azure-identity==1.19.0
hvac==2.3.0

pytest==9.1.1
pytest-asyncio==1.4.0
testcontainers[postgres]==4.8.1
respx==0.23.1
