fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
email-validator==2.2.0
pydantic-settings==2.5.2
sqlalchemy[asyncio]==2.0.35
asyncpg==0.30.0
alembic==1.13.3
redis==5.0.8
litellm==1.53.1
httpx==0.27.2
tenacity==9.0.0
pyjwt[crypto]==2.9.0
python-jose[cryptography]==3.3.0
python-multipart==0.0.9
structlog==24.4.0

# Secret providers (app/secrets/) -- each provider's SDK is imported lazily inside
# its own module, so a deployment only pays for the one it actually uses at
# runtime; all five are still installed so every provider is testable/importable.
# cryptography backs PostgresSecretProvider's at-rest encryption (Fernet); the
# others are external-service SDKs.
cryptography==43.0.3
boto3==1.35.68
google-cloud-secret-manager==2.21.1
azure-keyvault-secrets==4.9.0
azure-identity==1.19.0
hvac==2.3.0

pytest==8.3.3
pytest-asyncio==0.24.0
testcontainers[postgres]==4.8.1
respx==0.21.1
